{% extends 'base.html.twig' %}

{% block title %}FormReservation{% endblock %}

{% block body %}
    <h1>FormReservation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ form_reservation.id }}</td>
            </tr>
            <tr>
                <th>NombreVoyageur</th>
                <td>{{ form_reservation.nombreVoyageur }}</td>
            </tr>
            <tr>
                <th>Message</th>
                <td>{{ form_reservation.message }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_form_reservation_index') }}">back to list</a>

    <a href="{{ path('app_form_reservation_edit', {'id': form_reservation.id}) }}">edit</a>

    {{ include('form_reservation/_delete_form.html.twig') }}
{% endblock %}
