{% extends 'base.html.twig' %}

{% block title %}Edit Voyage
{% endblock %}

{% block body %}
	<h1>Édit Voyage</h1>

	{{ include('voyage/_form.html.twig', {'button_label': 'Update'}) }}

	{{ include('voyage/_delete_form.html.twig') }}

	<a href="{{ path('app_voyage_index') }}" class="btn btn-primary m-2">Retourner à la liste</a>
{% endblock %}
